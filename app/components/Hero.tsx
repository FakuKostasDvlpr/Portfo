"use client"

import Template from "./Template";

export default function Header() {
    return (
    <section className="flex m-auto w-full max-w-[1124px] items-center justify-between">
        <Template/>
    </section>
    );
}